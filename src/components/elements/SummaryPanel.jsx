export default function SummaryPanel({ rows = [] }) {
  return (
    <div className="bg-gray-100 p-4 rounded mb-4">
      {/* <p><strong>Active Projects:</strong> {activeCount}</p>
      <p><strong>Organizations:</strong> {organizations.map(o => o.name).join(', ')}</p>
      <p><strong>Top Staffed Projects:</strong></p>
      <ul>
        {topStaffed.map(p => (
          <li key={p.project.slug}>
            {p.project.title} ({p.staffCount} staff)
          </li>
        ))}
      </ul> */}
    </div>
  )
}
